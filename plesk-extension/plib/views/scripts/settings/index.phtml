<?php
/** @var $this Zend_View */
$mainUrl     = $this->mainUrl;
$saveUrl     = $this->saveUrl;
$testUrl     = $this->testUrl;
$hubUrl      = $this->hubUrl;
$hubUsername = $this->hubUsername;
$hasPassword = $this->hasPassword;
?>
<style>
.ahs-wrap { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; color: #1e293b; max-width: 600px; }
.ahs-topbar { display: flex; align-items: center; gap: .75rem; margin-bottom: 1.5rem; }
.ahs-topbar h2 { margin: 0; font-size: 1.25rem; font-weight: 700; color: #0f172a; }
.ahs-back { color: #0891b2; text-decoration: none; font-size: .875rem; }
.ahs-back:hover { text-decoration: underline; }
.ahs-card { background: #fff; border: 1px solid #e2e8f0; border-radius: 10px; padding: 1.5rem; }
.ahs-group { margin-bottom: 1.25rem; }
.ahs-group label { display: block; font-size: .8rem; font-weight: 600; color: #475569;
    text-transform: uppercase; letter-spacing: .04em; margin-bottom: .4rem; }
.ahs-group input { width: 100%; padding: .55rem .75rem; border: 1px solid #cbd5e1; border-radius: 6px;
    font-size: .9rem; background: #f8fafc; box-sizing: border-box; }
.ahs-group input:focus { outline: 2px solid #0891b2; border-color: transparent; background: #fff; }
.ahs-hint { font-size: .78rem; color: #64748b; margin-top: .3rem; }
.ahs-actions { display: flex; gap: .75rem; margin-top: 1.5rem; align-items: center; }
.ahs-btn { display: inline-flex; align-items: center; gap: .4rem; padding: .5rem 1.1rem;
    border-radius: 6px; border: 1px solid #cbd5e1; background: #fff; color: #334155;
    font-size: .875rem; cursor: pointer; transition: all .15s; }
.ahs-btn:hover { background: #f1f5f9; border-color: #94a3b8; }
.ahs-btn-primary { background: #0891b2; border-color: #0891b2; color: #fff; }
.ahs-btn-primary:hover { background: #0e7490; border-color: #0e7490; }
.ahs-result { font-size: .875rem; padding: .4rem .75rem; border-radius: 6px; }
.ahs-result-ok  { background: #dcfce7; color: #166534; }
.ahs-result-err { background: #fee2e2; color: #991b1b; }
</style>

<div class="ahs-wrap">
    <div class="ahs-topbar">
        <a href="<?= htmlspecialchars($mainUrl) ?>" class="ahs-back">&#8592; Back</a>
        <h2>Automation Hub — Settings</h2>
    </div>

    <form method="post" action="<?= htmlspecialchars($saveUrl) ?>">
        <div class="ahs-card">

            <div class="ahs-group">
                <label for="hub_url">Automation Hub URL</label>
                <input type="url" id="hub_url" name="hub_url"
                    value="<?= htmlspecialchars($hubUrl) ?>"
                    placeholder="https://ansible.johnsons.casa" required>
                <p class="ahs-hint">The base URL of your Automation Hub installation (no trailing slash).</p>
            </div>

            <div class="ahs-group">
                <label for="hub_username">Username</label>
                <input type="text" id="hub_username" name="hub_username"
                    value="<?= htmlspecialchars($hubUsername) ?>"
                    placeholder="admin" required autocomplete="off">
            </div>

            <div class="ahs-group">
                <label for="hub_password">Password<?= $hasPassword ? ' <span style="font-weight:400;color:#64748b">(leave blank to keep existing)</span>' : '' ?></label>
                <input type="password" id="hub_password" name="hub_password"
                    placeholder="<?= $hasPassword ? '••••••••' : 'Enter password' ?>"
                    autocomplete="new-password">
            </div>

            <div class="ahs-actions">
                <button type="submit" class="ahs-btn ahs-btn-primary">Save</button>
                <button type="button" class="ahs-btn" onclick="AHS.test()">Test Connection</button>
                <span id="ahs-test-result"></span>
            </div>

        </div>
    </form>
</div>

<script>
const AHS = (() => {
    const TEST_URL = <?= json_encode($testUrl) ?>;

    async function test() {
        const resultEl = document.getElementById('ahs-test-result');
        resultEl.className = 'ahs-result';
        resultEl.textContent = 'Testing…';

        const payload = {
            hub_url:      document.getElementById('hub_url').value.trim(),
            hub_username: document.getElementById('hub_username').value.trim(),
            hub_password: document.getElementById('hub_password').value,
        };

        try {
            const resp = await fetch(TEST_URL, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload),
            });
            const data = await resp.json();
            resultEl.className = 'ahs-result ' + (data.ok ? 'ahs-result-ok' : 'ahs-result-err');
            resultEl.textContent = data.message;
        } catch (e) {
            resultEl.className = 'ahs-result ahs-result-err';
            resultEl.textContent = 'Request failed: ' + e.message;
        }
    }

    return { test };
})();
</script>
